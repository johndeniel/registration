export const barangayOptions = [
    'Barangay 1', 'Barangay 2', 'Barangay 3', 
    'Barangay 4', 'Barangay 5', 'Barangay 6', 
    'Barangay 7', 'Barangay 8'
  ].map((barangay) => ({
    label: barangay,
    value: barangay
  }))
  
  export const applicationTypeOptions = [
    'New', 'Lost', 'Replacement', 'Transfer'
  ].map((type) => ({
    label: type,
    value: type
  }))
  
  export const sexOptions = [
    'Male', 'Female', 'Other'
  ].map((sex) => ({
    label: sex,
    value: sex
  }))
  
  export const civilStatusOptions = [
    'Single', 'Married', 'Divorced', 'Widowed'
  ].map((status) => ({
    label: status,
    value: status
  }))
  
  export const educationalAttainmentOptions = [
    'Elementary', 'High School', 'College', 
    'Post-Graduate', 'Vocational', 'Doctorate'
  ].map((attainment) => ({
    label: attainment,
    value: attainment
  }))
  
  export const relationshipOptions = [
    'Parent', 'Spouse', 'Sibling', 
    'Child', 'Friend', 'Guardian', 'Other'
  ].map((relationship) => ({
    label: relationship,
    value: relationship
  }))
  
  export const occupationOptions = [
    'Doctor', 'Engineer', 'Teacher', 
    'Lawyer', 'Nurse', 'Artist', 'Other'
  ].map((occupation) => ({
    label: occupation,
    value: occupation
  }))
  